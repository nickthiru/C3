/*
  Store all UserManagementService functions here. These are supposed to be pure functions.
  They will be called statically too.

  Remember to register an "event" at the end of each task/function to SNS.
*/

module.exports = {
  // Expose the functions as API
}